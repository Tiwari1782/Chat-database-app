<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Write New Chat</title>

    <style>
      :root {
        --wa-green: #075e54;
        --wa-green-2: #0b806f;
        --bg: #ece5dd;
        --card: #ffffff;
        --text: #1f2937;
        --muted: #6b7280;
        --border: #e5e7eb;
      }

      * {
        box-sizing: border-box;
      }

      body {
        margin: 0;
        font-family: "Segoe UI", system-ui, -apple-system, Arial, sans-serif;
        background: var(--bg);
        color: var(--text);
        min-height: 100vh;
        padding-top: 90px; /* space for fixed navbar */
        display: flex;
        justify-content: center;
        align-items: flex-start;
      }

      /* ===== NAVBAR ===== */
      .navbar {
        position: fixed;
        top: 0;
        left: 0;
        width: 100%;
        background: var(--wa-green);
        padding: 14px 22px;
        box-shadow: 0 6px 18px rgba(0, 0, 0, 0.18);
        z-index: 1000;
      }

      .navbar-inner {
        max-width: 980px;
        margin: 0 auto;
        display: flex;
        align-items: center;
        justify-content: space-between;
        gap: 12px;
      }

      .navbar h2 {
        margin: 0;
        color: white;
        font-size: 20px;
        font-weight: 600;
        letter-spacing: 0.2px;
      }

      .btn {
        border: none;
        cursor: pointer;
        border-radius: 10px;
        font-weight: 700;
        font-size: 13px;
        padding: 8px 12px;
        transition: transform 0.18s ease, box-shadow 0.18s ease,
          background-color 0.18s ease, border-color 0.18s ease;
        display: inline-flex;
        align-items: center;
        gap: 8px;
        line-height: 1;
        user-select: none;
        text-decoration: none;
      }

      .btn:active {
        transform: translateY(0);
        box-shadow: none;
      }

      .btn-secondary {
        background: rgba(255, 255, 255, 0.14);
        color: white;
        border: 1px solid rgba(255, 255, 255, 0.25);
      }

      .btn-secondary:hover {
        background: rgba(255, 255, 255, 0.2);
        transform: translateY(-1px);
      }

      .btn-primary {
        width: 100%;
        background: var(--wa-green-2);
        color: white;
        box-shadow: 0 10px 22px rgba(0, 0, 0, 0.18);
        padding: 11px 14px;
        font-size: 14px;
      }

      .btn-primary:hover {
        background: #0f8f7a;
        transform: translateY(-2px);
        box-shadow: 0 14px 26px rgba(0, 0, 0, 0.22);
      }

      /* ===== FORM CARD ===== */
      .form-card {
        width: 100%;
        max-width: 520px;
        margin: 0 16px;
        background: var(--card);
        padding: 20px 20px 18px;
        border-radius: 18px;
        box-shadow: 0 14px 35px rgba(0, 0, 0, 0.14);
        border: 1px solid rgba(0, 0, 0, 0.04);
      }

      .form-card h3 {
        margin: 0;
        color: var(--text);
        font-size: 18px;
        font-weight: 800;
        letter-spacing: 0.2px;
      }

      .sub {
        margin: 8px 0 14px;
        color: var(--muted);
        font-size: 13px;
        line-height: 1.45;
      }

      .divider {
        border: none;
        border-top: 1px solid var(--border);
        margin: 14px 0;
      }

      /* ===== FIELDS ===== */
      .field {
        margin-top: 12px;
      }

      label {
        display: block;
        margin-bottom: 8px;
        font-size: 13px;
        font-weight: 800;
        color: #111827;
      }

      input,
      textarea {
        width: 100%;
        padding: 11px 12px;
        border-radius: 14px;
        border: 1px solid #cbd5e1;
        font-size: 14px;
        box-sizing: border-box;
        font-family: inherit;
        transition: border-color 0.18s ease, box-shadow 0.18s ease;
        background: #fff;
      }

      textarea {
        resize: none;
        min-height: 110px;
        line-height: 1.45;
      }

      input:focus,
      textarea:focus {
        outline: none;
        border-color: var(--wa-green);
        box-shadow: 0 0 0 3px rgba(7, 94, 84, 0.14);
      }

      .helper {
        margin: 8px 0 0;
        color: var(--muted);
        font-size: 12px;
      }

      .actions {
        display: flex;
        gap: 10px;
        justify-content: flex-end;
        margin-top: 16px;
      }

      .btn-light {
        background: #f3f4f6;
        border: 1px solid #e5e7eb;
        color: #111827;
        width: auto;
        box-shadow: none;
      }

      .btn-light:hover {
        background: #eaecef;
        transform: translateY(-1px);
      }

      @media (max-width: 520px) {
        .actions {
          flex-direction: column;
        }
        .btn-light,
        .btn-primary {
          width: 100%;
          justify-content: center;
        }
      }
    </style>
  </head>

  <body>
    <div class="navbar">
      <div class="navbar-inner">
        <h2>Add New Message</h2>
        <a class="btn btn-secondary" href="/chats">← Back</a>
      </div>
    </div>

    <div class="form-card">
      <h3>Write New Chat</h3>
      <p class="sub">Send a message by entering the sender, message, and receiver.</p>

      <hr class="divider" />

      <form action="/chats" method="post" autocomplete="off">
        <div class="field">
          <label for="from">From</label>
          <input id="from" name="from" placeholder="e.g., Tony" required />
          <p class="helper">This will appear as the sender name.</p>
        </div>

        <div class="field">
          <label for="message">Message</label>
          <textarea
            id="message"
            name="message"
            placeholder="Write your message here..."
            required
          ></textarea>
          <p class="helper">Keep it short and clear.</p>
        </div>

        <div class="field">
          <label for="to">To</label>
          <input id="to" name="to" placeholder="e.g., Steve" required />
          <p class="helper">Receiver name shown on the chat card.</p>
        </div>

        <div class="actions">
          <a class="btn btn-light" href="/chats">Cancel</a>
          <button class="btn btn-primary" type="submit">Create Chat</button>
        </div>
      </form>
    </div>
  </body>
</html>